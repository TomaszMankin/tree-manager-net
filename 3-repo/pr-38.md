Closes #13

## What
Adds LineageFolderGenerator service (Core/Services) implementing ILineageFolderGenerator (Core/Abstractions/Services). ComputeLineages groups tree members by contributor's lineage surname (maiden if present, else last name); WalkAncestors unconditionally follows parent links with no surname gate (per ADR-021). Members include universal subtree + contributor + contributor spouses + full ancestor bloodline. IFolderTreeGenerator.ComputeMembership promoted to interface; filename tokens reused from Drzewo membership (single-hourglass). FolderTreeNaming.Deduplicate extracted as shared static helper. GenerateLineageFolders command in MainViewModel; "Generuj Rody" button added to MainWindow.xaml. DI registration in App.xaml.cs. 348 tests pass (327→348, +21 L0 Core + L1 Infra + L0 App).

## Links
- Issue: #13 (agent spec + acceptance criteria)
- ADRs: ADR-016, ADR-021 (inherited from py; no new ADR required)

## Checklist
- [x] `dotnet test --filter "Tier=L0"` passes
- [x] L1 tests added (LineageFolderGeneratorIntegrationTests)
- [x] ADR written (none required; reuses py lineage decisions)
- [x] Issue `Done when` items checked off
