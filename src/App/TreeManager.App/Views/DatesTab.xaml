<UserControl x:Class="TreeManager.App.Views.DatesTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TreeManager.App.ViewModels"
             xmlns:views="clr-namespace:TreeManager.App.Views"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:DatesTabViewModel}">
    <StackPanel Margin="32,24">

        <TextBlock Text="Data urodzin" Style="{StaticResource SectionHeader}"/>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <CheckBox Content="Przed" IsChecked="{Binding BirthDate.IsBefore}" Margin="0,0,16,0"/>
            <CheckBox Content="Około" IsChecked="{Binding BirthDate.IsApprox}"/>
        </StackPanel>
        <views:OptionalDatePickerControl DataContext="{Binding BirthDate}" Margin="0,0,0,24"/>

        <Separator Margin="0,0,0,20" Background="#E0E0E0"/>

        <CheckBox Content="Czy zmarł(a)?"
                  IsChecked="{Binding IsDeceased}"
                  Margin="0,0,0,16" FontSize="14"/>

        <StackPanel Visibility="{Binding IsDeceased, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Data śmierci" Style="{StaticResource SectionHeader}"/>
            <StackPanel Orientation="Horizontal" IsEnabled="{Binding IsDeceased}" Margin="0,0,0,8">
                <CheckBox Content="Przed" IsChecked="{Binding DeathDate.IsBefore}" Margin="0,0,16,0"/>
                <CheckBox Content="Około" IsChecked="{Binding DeathDate.IsApprox}"/>
            </StackPanel>
            <views:OptionalDatePickerControl DataContext="{Binding DeathDate}"/>
        </StackPanel>

    </StackPanel>
</UserControl>
