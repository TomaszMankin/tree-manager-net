<UserControl x:Class="TreeManager.App.Views.OptionalDatePickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TreeManager.App.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:OptionalDatePickerViewModel}"
             IsEnabled="{Binding IsEnabled}">
    <StackPanel Orientation="Horizontal">
        <ComboBox Width="70" Margin="0,0,8,0"
                  ItemsSource="{Binding DayOptions}"
                  SelectedItem="{Binding Day, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource NullableIntToStringConverter}, ConverterParameter=dd}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <ComboBox Width="70" Margin="0,0,8,0"
                  ItemsSource="{Binding MonthOptions}"
                  SelectedItem="{Binding Month, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource NullableIntToStringConverter}, ConverterParameter=mm}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <ComboBox Width="90"
                  ItemsSource="{Binding YearOptions}"
                  SelectedItem="{Binding Year, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource NullableIntToStringConverter}, ConverterParameter=yyyy}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </StackPanel>
</UserControl>
